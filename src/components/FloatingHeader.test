// FloatingHeader.js
import { defineProperties } from "figma:react";

// Une petite icône SVG pour le logo
const LogoIcon = () => (
  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
    <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
    <path d="M2 17L12 22L22 17" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
    <path d="M2 12L12 17L22 12" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
  </svg>
);

export default function FloatingHeader({ projectName }) {
  return (
    // Conteneur principal: fixé en haut, pleine largeur, avec un z-index élevé
    <div className="fixed top-4 left-1/2 -translate-x-1/2 w-[95%] max-w-4xl z-50">
      
      {/* Le header lui-même avec l'effet de verre dépoli */}
      <div className="flex items-center justify-between h-16 px-6 bg-white/70 backdrop-blur-md rounded-xl shadow-md border border-gray-200/80">
        
        {/* Partie gauche: Logo et nom du projet */}
        <div className="flex items-center gap-3">
          <div className="text-blue-600">
            <LogoIcon />
          </div>
          <span className="text-lg font-bold text-gray-800">{projectName}</span>
        </div>

        {/* Partie droite: Navigation et bouton d'action */}
        <div className="flex items-center gap-6">
          <a href="#" className="text-sm font-medium text-gray-600 hover:text-gray-900 transition-colors">Bons Plans du Moment</a>
          <a href="#" className="text-sm font-medium text-gray-600 hover:text-gray-900 transition-colors">Mes voyages</a>
          <button className="px-4 py-2 text-sm font-semibold text-white bg-blue-600 rounded-lg hover:bg-blue-700 transition-colors">
            Se connecter
          </button>
        </div>

      </div>
    </div>
  );
}

// Propriété pour changer le nom du projet depuis Figma
defineProperties(FloatingHeader, {
  projectName: {
    type: "string",
    defaultValue: "Nom du Projet",
  },
});