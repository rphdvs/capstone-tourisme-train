// DestinationCard.js

export function DestinationCard({ imageUrl, cityName, price }) {
  return (
    <div className="flex items-center h-24 rounded-lg overflow-hidden shadow-sm bg-white border border-gray-200">
      <img
        src={imageUrl}
        alt={`Image de ${cityName}`}
        className="w-24 h-full object-cover"
      />
      <div className="flex-grow p-4">
        <h3 className="font-bold text-gray-800">{cityName}</h3>
        <p className="text-gray-600">à partir de {price}€</p>
      </div>
    </div>
  );
}